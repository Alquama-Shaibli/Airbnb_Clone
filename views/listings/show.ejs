<% layout("layouts/boilerplate") %>

  <div class="row mt-3">
    <div class="col-8 offset-2">
      <h3> <%= listing.title %> </h3>

      <div class="card col-6 offset-3 show card listing-card">
        <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="listing_image" />
        <div class="card-body">
            <p class="card-text">
            <p class="card-text"><i><%= listing.owner.username %></i></p>
              <br>
          <p class="card-text"> <%= listing.description %></p>
          </p>
          <p class="card-text">&#8377; <%= listing.price?.toLocaleString("en-IN") ?? "N/A" %>
          </p>
          <p class="card-text">
            <%= listing.location %>
          </p>
          <p class="card-text">
            <%= listing.country %>
          </p>
        </div>
      </div>
      <br/>
      
     <%= if (currentUser && currentUser._id.equal(listing.owner._id)) { %>

      <div class="btns">
        <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark show-btn">Edit</a>
        <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
          <button class="btn btn-danger show-btn">Delete</button>
        </form>
      </div>
      <% } %>

      <div class ="col-8 offset-3">
        <hr/>


    <%= if (currentUser) { %>
       <h4>Leave a Review</h4>

       <form class="mb-3 mt-3 needs-validation" action="/listings/<%= listing._id %>/reviews" method="POST" novalidate>
        <div class="mb-3 mt-3">
          <label for="review[rating]" class="form-label">Rating: <span id="ratingValue">3</span> / 5</label>
          <input type="range" id="review[rating]" name="review[rating]" min="1" max="5" value="3" required class="form-range"
            oninput="document.getElementById('ratingValue').textContent = this.value">
          <div class="invalid-feedback">
            Please provide a rating.
          </div>
        </div>
  
        <fieldset class="starability-slot">

  <legend>First rating:</legend>
  <input type="radio" id="no-rate" class="input-no-rate" name="rating" value="0" checked aria-label="No rating." />
  <input type="radio" id="first-rate1" name="rating" value="1" />
  <label for="first-rate1" title="Terrible">1 star</label>
  <input type="radio" id="first-rate2" name="rating" value="2" />
  <label for="first-rate2" title="Not good">2 stars</label>
  <input type="radio" id="first-rate3" name="rating" value="3" />
  <label for="first-rate3" title="Average">3 stars</label>
  <input type="radio" id="first-rate4" name="rating" value="4" />
  <label for="first-rate4" title="Very good">4 stars</label>
  <input type="radio" id="first-rate5" name="rating" value="5" />
  <label for="first-rate5" title="Amazing">5 stars</label>
</fieldset>

        <div class="mb-3 mt-3">
          <label for="review[comment]" class="form-label">Comment:</label>
          <textarea id="review[comment]" name="review[comment]"  rows="4" class="form-control" required></textarea>
          <div class="invalid-feedback">
            Please enter a comment.
          </div>
        </div>
        <button type="submit" class="btn btn-outline-dark">Submit Review</button>
       </form>

      </div>
<% } %>
  <hr/>
  <p> <b>All Reviews</b></p>
  <div class="row">
  <% if (listing.reviews.length === 0) { %>
    <p class="text-muted ms-3">No reviews yet. Be the first to leave one!</p>
  <% } %>
  <% for(let review of listing.reviews) { %>
     <div class="card col-5 ms-3 mb-3">
      <div class="card-body">
         <h5 class="card-title">Review</h5>
         <p class="card-text"><b>
            <%= review.comment %>
          </b></p>
        <p class="card-text">
         @ <%= review.author.username %>
        </p>
        <div class="mb-3 mt-3">
        </div>
        <p class="starability-result card-text" data-rating="<%= review.rating %>">
    Rated: <%= review.rating %> stars
  </p>
      </div>
      <form action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" method="POST" class="mb-3 mt-3">
        <button class="btn btn-sm btn-dark">Delete</button>
      </form>
     </div>
  <% } %>
    </div>
    </div>
  </div>